#pragma once

namespace DIMS
{
	//Actions probably need to be using this now. it will carry changeable flags, as well containing
	// a reference to the out input_smart pointer.... ACTUALLY, I just realized I may need more to do that.
	// namely, what if a bunch of events always fire some shit off?

	//Still, this would be maybe important.
	struct EventResult
	{
		
	};
}